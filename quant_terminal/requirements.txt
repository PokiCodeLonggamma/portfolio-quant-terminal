# Streamlit Cloud deployment requirements
# Mirrors quant_terminal/pyproject.toml [project].dependencies — keep in sync.
# Streamlit Cloud installs this file from the same dir as the main file (app.py).

pandas>=2.0
numpy>=1.26
scipy>=1.12
statsmodels>=0.14
scikit-learn>=1.4
plotly>=5.20
streamlit>=1.32
streamlit-lightweight-charts>=0.7.20
lightweight-charts>=2.0
yfinance>=0.2.40
alpaca-py>=0.21
fredapi>=0.5
pyyaml>=6.0
requests>=2.31
python-dotenv>=1.0
openpyxl>=3.1
pyarrow>=15.0
pydantic>=2.5
httpx>=0.27
feedparser>=6.0
streamlit-autorefresh>=1.0
anthropic>=0.40

# Vendored legacy short-squeeze scanner (vendor/legacy_squeeze/)
beautifulsoup4>=4.12
lxml>=5.0
rich>=13.0

# Dev / tests (kept for parity with editable install)
pytest>=8.0
