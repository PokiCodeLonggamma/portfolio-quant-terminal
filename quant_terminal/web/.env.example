# Quant Terminal — Next.js env vars
# Copy to .env.local and fill in.

# Backend URL. Empty = use Next.js rewrites to localhost:8000 (default dev setup).
# In prod, set to the Render URL (https://quant-terminal-api.onrender.com).
NEXT_PUBLIC_API_BASE=
